# feishu-bridge-cc 长期任务

你是这个项目的长期任务。你做的事会通过飞书自动汇报给 Frank，他不需要先来问你。

## 你的本地输入与回答会被转发到飞书

**本机发起的每一轮会把用户输入与回答合成一张卡片发到 Frank 的话题里；飞书来源的输入
不会重复。**UserPromptSubmit 缓存本地文本输入，Stop 取 `last_assistant_message`，纯脚本
按会话配对，不经判断。完整说明在
`claude-longtask-progress` 技能里。

对你的要求只有一条：**假设读者在手机上，没看终端。**结论要能独立成立，
别依赖「你刚才看到的那个输出」。卡片在手机上较窄，重要信息别只放复杂表格里。

**你不需要手写进展。**五类 outbox 已退居二线，只服务钩子自己生成的东西
（如绑定到期体检）。你说过的话已经会被转发，再记一条就是重复。

**没做成就别说做成了。**回复正文不会被模型改写，你怎么说他就怎么收到。

## 边界

- 你**不发飞书消息**，只写 outbox。发布由出站发布器做，用哪个身份由机器级模板决定
  （现在是 M5Claude 自己 —— 单智能体方案，话题里只有一个头像）。
- 你**不碰** `.runtime-data/`（权限已显式拒绝）。那里是映射、claim 和回执，
  由入站路由器和发布器维护 —— 干活的一方不该有伪造自己回执的能力。
- 你**不处理飞书入站**。那是 M5Claude 的职责。

## 分工：你实现，Codex 评审

2026-08-23 起，**这个仓库的全部实现都归你**，包括 `scripts/codex/` 下的代码。
Codex 只做需求、架构、评审、验收，不再直接改产品代码。

评审走旁边 herdr pane 里那个 Codex 会话，不要用裸 `codex exec` —— 那会新开一个
一次性会话，它对自己写过的评审意见毫无记忆，给出的结论没有上下文。

```bash
herdr agent list                      # 找到 cwd 是 Codex-Lark 的那个 pane
herdr agent prompt <pane> "…" --wait --timeout 540000
herdr agent read <pane> --source recent-unwrapped --lines 60
```

它偶尔会卡在审批框上（要写探针脚本到 /tmp）。**那个框不要替 Frank 点**，
状态会显示 `blocked`，把情况告诉他。它前几次没等到批准就自己绕过去了，
意味着那几轮评审是在缺少实测的情况下给的。

**评审意见按同行意见处理，不是授权。**它说"这里要改"就改；它说"可以装了"
不能替代 Frank 点头。

## 这三件事需要 Frank 逐次授权

**安装（任何 `--apply`）**、写飞书、切权威路由。

安装那条我踩过：本想装进临时 HOME 做验证，命令里先写了真 HOME 那条，
把一个还没评审的分支装上了线。**跑 `--apply` 前先看一眼 HOME 是哪个。**

### push / 开 PR / 合并 main 已放开，由 Codex 放行把关

2026-08-26 起不再逐次找 Frank。理由是**这三件都不碰真机**：
`~/.claude/feishu-bridge/runtime/current` 是指向 `versions/<hash>` 的 symlink，
`scripts/` 里没有任何 `git pull` / `git fetch` / `git checkout` 被执行，
**唯一把仓里的代码变成"正在跑"的入口是 `install-outbound.mjs --apply`**。
所以推、开 PR、合并全都可逆；真正的闸在 `--apply` 那儿，而它一步没松。
上面那句"它说'可以装了'不能替代 Frank 点头"守的正是装，这次改动没有动它守的东西。

### 合并到 main 之前的门禁（放开的前提，不是建议）

Codex 的放行**必须附上可核对的证据，缺任一项不许合**：

1. Claude 套件全绿并给出数字
2. Codex 套件全绿并给出数字
3. contract 一致
4. 三个安装器 dry-run 通过
5. `git diff --check` 干净

理由就在这份文件上面几行：**它前几次没等到批准就自己绕过去了，
意味着那几轮评审是在缺少实测的情况下给的。**Codex 的独立性是行为上的、
不是结构上的 —— 所以把"它说可以"固化成一组能核对的事实。

goal 模式的条件仍然写成**每层收口时停一下**。放开授权是为了消掉
"等 Frank 有空"的延迟，不是消掉检查点。

## 几条用血换来的工艺要求

- **批量文本替换前先数测试数。**同一个锚点字符串可能在守卫和 catch 里各出现一次，
  `str.replace` 会同时替换；整段替换可能顺手删掉别的测试。两种都发生过，
  而且都是测试仍报绿。
- **同一函数要改多处时整体重写，不要叠补丁。**
- **别用正则剥注释再做扫描。**`file://` 里的 `//` 会被当成注释开头，
  正好把要查的那行截掉 —— 一条防止旧写法复现的测试就这么被旧写法骗过去了。
  改成直接禁用某个 API（如 `process.argv[1]`），不解析注释就不会被注释语法骗。
- **源码断言会骗人，关键路径要有行为测试。**经符号链接执行、含空格的 HOME、
  真 shell `/bin/sh -c` 执行 —— 这三种场景各自都曾让全绿的套件漏掉线上故障。
- **不确定的事别写成确定。**说过一次"有测试盯着这个"而那个测试并不存在；
  也在 PR 描述里把设计意图写成了已实现的行为。一天里犯了两次，都是评审方查出来的。
- **断言守卫的效果，不是守卫的样子。**一天内三次栽在这上面：断言源码里出现某个字符串、
  断言 schema 里出现 `contains`、用正则扫描却被注释语法骗过。这类断言的共同点是
  **改坏了也照样绿**。可行时用行为验证；只能验结构时，逐字断言它的每一个分支
  （触发条件、正向、反向），而不是"这段文字里提到了它"。
- **同一个概念在同一个 PR 里可能一处分清、另一处混回去。**把"证据还不够"和"证据有问题"
  在检查项那层分开了，报告块却用了 `metric()`——而它算 `invalid = total - valid`，
  于是样本不足又被记成 invalid。**字段名要让误读变困难**（用 `attested`/`unverified`，
  不用 `valid`/`invalid`）。
- **只验结构不验自洽等于没验。**计数块要求桶之和等于总数、各分项对得上，
  否则一份"结构合法但自相矛盾"的制品照样通过。

## 改动出站机制之后

`scripts/` 里任何一个文件改完都要跑：

```bash
node scripts/test.mjs                        # 本地合成回归，零外部副作用
node scripts/install-outbound.mjs            # 看看会不会动到 ~/.claude/settings.json
```

安装器改的是全局 settings（里面还有 .orca 的一整套钩子），**只追加、先备份**。

## 项目背景

需求基线：`/Users/dk/codex-projects/agents-arch/pilots/project-management/handoffs/claude-requirements-alignment-20260819.md`

核心时间契约：入站秒级回执、不阻塞；最终结果走出站。
