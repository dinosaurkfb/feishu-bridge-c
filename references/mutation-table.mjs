/**
 * 真实变异表（m1506–m1563，58 条）—— 维护门 PR C 第 2 步与 #85/#86 收口时逐条验证过全部转红。
 * killedBy 是预期击杀测试名子串（给 runner 的定向加速用；定向没红会自动升级全量，不是判据）。
 * 历史击杀记录：会话 scratchpad 的 mutation-full-48.log 等（tmp 会被清，本文件才是持久资产）。
 */
export const MUTATIONS = [
  {"id":"m1506","file":"scripts/maintenance/render-artifacts.mjs","find":"if (xpb.exists) push(\"codex\", codexPlistFile, \"plist\", codexPlistBody({ home, node: codexNode, codexHome }), xpb, false);","replace":"push(\"codex\", codexPlistFile, \"plist\", codexPlistBody({ home, node: codexNode, codexHome }), xpb, false);","killedBy":"PR C 第 2 步"},
  {"id":"m1507","file":"scripts/maintenance/render-artifacts.mjs","find":"sha256: artifactSha({ kind: a.kind, text: a.bytes","replace":"sha256: artifactSha({ kind: \"skill\", text: a.bytes","killedBy":"PR C 第 2 步"},
  {"id":"m1508","file":"scripts/maintenance/maintenance-install-core.mjs","find":"if (!sameState(stateOf(live), a.base)) return { ok: false, reason: \"base_changed\", path: a.path,","replace":"if (false) return { ok: false, reason: \"base_changed\", path: a.path,","killedBy":"PR C 第 2 步"},
  {"id":"m1509","file":"scripts/maintenance/maintenance-install-core.mjs","find":"if (live.exists) { backup = path.join(staged.dir, \"backups\", String(i)); meta = writeBackup(backup, live.bytes); }","replace":"if (false) { backup = path.join(staged.dir, \"backups\", String(i)); meta = writeBackup(backup, live.bytes); }","killedBy":"PR C 第 2 步"},
  {"id":"m1510","file":"scripts/maintenance/maintenance-install-core.mjs","find":"backup_bytes: meta.bytes, intended_after: { exists: true, sha256: sha256(bytes) } } })));","replace":"backup_bytes: meta.bytes, intended_after: { exists: true, sha256: sha256(Buffer.from(\"x\")) } } })));","killedBy":"PR C 第 2 步"},
  {"id":"m1511","file":"scripts/maintenance/maintenance-install-core.mjs","find":"if (probe.status !== 0 || decision?.decision !== \"block\") bad.push(\"Codex 阻断探针不过","replace":"if (false) bad.push(\"Codex 阻断探针不过","killedBy":"PR C 第 2 步"},
  {"id":"m1512","file":"scripts/maintenance/operation.mjs","find":"try { fs.rmSync(stagedDirPath(ctx, token), { recursive: true, force: true }); }","replace":"try { void 0; }","killedBy":"PR C 第 2 步"},
  {"id":"m1513","file":"scripts/maintenance/operation.mjs","find":"const restored = restoreInstallWrites(ctx, p.doc, { note: (t) => addNote({ dir: ctx.dir, token, lease, note: t, now: ctx.now() }) });","replace":"const restored = { incomplete: [], fatal: null };","killedBy":"PR C 第 2 步"},
  {"id":"m1514","file":"scripts/maintenance/operation.mjs","find":"if (restored.incomplete.length > 0) {","replace":"if (false) {","killedBy":"PR C 第 2 步"},
  {"id":"m1515","file":"scripts/maintenance/operation.mjs","find":"if (mode === \"rollback\") currentSteps.reverse();","replace":"/* m1515 */","killedBy":"PR C 第 2 步"},
  {"id":"m1516","file":"scripts/maintenance/journal.mjs","find":"const INSTALL_DONE = Object.freeze([...STAGED_DONE, \"current:claude:install\", \"current:codex:install\", \"receipt:claude\", \"receipt:codex\"]);","replace":"const INSTALL_DONE = Object.freeze([...STAGED_DONE, \"current:claude:install\", \"current:codex:install\"]);","killedBy":"PR C 第 2 步"},
  {"id":"m1517","file":"scripts/maintenance/journal.mjs","find":"if (!idOk || !/^(claude|codex)(:install)?$/u.test(rest)) return \"current 的 id","replace":"if (!idOk) return \"current 的 id","killedBy":"PR C 第 2 步"},
  {"id":"m1518","file":"scripts/maintenance/maintenance-install-core.mjs","find":"if (sha256(f.buf) !== a.intendedAfterSha) return { ok: false, reason: \"staged_artifact_drifted\", path: a.path };","replace":"if (false) return { ok: false, reason: \"staged_artifact_drifted\", path: a.path };","killedBy":"PR C 第 2 步"},
  {"id":"m1519","file":"scripts/maintenance/maintenance-install-core.mjs","find":"if (gate.state !== \"active\" || gate.payload.token !== token) return { ok: false, reason: \"gate_not_ours\"","replace":"if (false) return { ok: false, reason: \"gate_not_ours\"","killedBy":"PR C 第 2 步"},
  {"id":"m1520","file":"scripts/maintenance/maintenance-install-core.mjs","find":"if (live !== want) return { ok: false, reason: \"current_unbound\"","replace":"if (false) return { ok: false, reason: \"current_unbound\"","killedBy":"PR C 第 2 步"},
  {"id":"m1521","file":"scripts/maintenance-install.mjs","find":"const rb = rollbackOperation(c, token, lease);","replace":"const rb = { ok: true, activeCleared: true, phase: \"rolled_back\" };","killedBy":"PR C 第 2 步"},
  {"id":"m1522","file":"scripts/maintenance/operation.mjs","find":"if (!v.ok) return \"备份核不过：\" + v.why + \"（\" + st.backup + \"）\";","replace":"if (false) return \"备份核不过：\" + v.why + \"（\" + st.backup + \"）\";","killedBy":"PR C 第 2 步"},
  {"id":"m1523","file":"scripts/maintenance/journal.mjs","find":"if (lease.path !== leasePath(dir, token)) return { ok: false, reason: \"lease_mismatch\"","replace":"if (false) return { ok: false, reason: \"lease_mismatch\"","killedBy":"PR C 单元"},
  {"id":"m1524","file":"scripts/install-outbound.mjs","find":"const g = gateBlocks();\n  if (g.blocked)","replace":"const g = gateBlocks();\n  if (false)","killedBy":"PR C 第 2 步"},
  {"id":"m1525","file":"scripts/install-inbound.mjs","find":"const g = gateBlocks();\n  if (g.blocked)","replace":"const g = gateBlocks();\n  if (false)","killedBy":"PR C 第 2 步"},
  {"id":"m1526","file":"scripts/codex/install.mjs","find":"const g = gateBlocks();\n  if (g.blocked)","replace":"const g = gateBlocks();\n  if (false)","killedBy":"PR C 第 2 步"},
  {"id":"m1527","file":"scripts/runtime-install.mjs","find":"if (live !== expectBefore) return { ok: false, reason: \"current_changed\", live };","replace":"if (false) return { ok: false, reason: \"current_changed\", live };","killedBy":"PR C 第 2 步"},
  {"id":"m1528","file":"scripts/maintenance/operation.mjs","find":"if (live !== st.intended_after) incomplete.push({ id: st.id, why: \"成功路径 reopening 时 current=\" + String(live) + \" 不是目标 \" + st.intended_after + \"，不删门\" });","replace":"if (false) incomplete.push({ id: st.id, why: \"成功路径 reopening 时 current=\" + String(live) + \" 不是目标 \" + st.intended_after + \"，不删门\" });","killedBy":"PR C 第 2 步"},
  {"id":"m1529","file":"scripts/maintenance/operation.mjs","find":"if (live === null || !sameFileState(live, st.intended_after)) incomplete.push({ id: st.id, why: \"现场\" + (live === null ? \"读不出\" : \"与 journal 的目标状态不符\") + \"，不删门\" });","replace":"if (false) incomplete.push({ id: st.id, why: \"现场\" + (live === null ? \"读不出\" : \"与 journal 的目标状态不符\") + \"，不删门\" });","killedBy":"PR C 第 2 步"},
  {"id":"m1530","file":"scripts/maintenance/operation.mjs","find":"  if (incomplete.length > 0) return bail({});\n  // ③b 全部对得上才删 staged 私有目录","replace":"  // ③b 全部对得上才删 staged 私有目录","killedBy":"PR C 第 2 步"},
  {"id":"m1531","file":"scripts/maintenance/maintenance-install-core.mjs","find":"if (sha256(r.buf) !== expect.sha256) return { ok: false, reason: \"staged_plan_mismatch\", why: \"plan.json 的 sha 与 journal 锚不符\" };","replace":"if (false) return { ok: false, reason: \"staged_plan_mismatch\", why: \"plan.json 的 sha 与 journal 锚不符\" };","killedBy":"PR C 第 2 步"},
  {"id":"m1532","file":"scripts/maintenance/maintenance-install-core.mjs","find":"if (a.file !== path.join(\"artifacts\", String(i))) return \"file 必须是 artifacts/<下标>：\" + String(a.file);","replace":"if (false) return \"file 必须是 artifacts/<下标>：\" + String(a.file);","killedBy":"PR C 第 2 步"},
  {"id":"m1533","file":"scripts/maintenance/maintenance-install-core.mjs","find":"for (const a of staged.artifacts) if (!allow[a.chain]?.has(a.path)) bad.push(\"制品路径不在安装面 allowlist：\" + a.chain + \" \" + a.path);","replace":"for (const a of staged.artifacts) void a;","killedBy":"PR C 第 2 步"},
  {"id":"m1534","file":"scripts/maintenance/maintenance-install-core.mjs","find":"else if (locked.lockUncleared) { failWhy","replace":"else if (false) { failWhy","killedBy":"PR C 第 2 步"},
  {"id":"m1535","file":"scripts/maintenance/operation.mjs","find":"    const f = noted(st.id + \" 已按备份回退\");\n    if (f !== null) return { incomplete, fatal: f };","replace":"    noted(st.id + \" 已按备份回退\");","killedBy":"PR C 第 2 步"},
  {"id":"m1536","file":"scripts/install-outbound.mjs","find":"holdInstallSurfaceLockOrExit();","replace":";","killedBy":"PR C 第 2 步"},
  {"id":"m1537","file":"scripts/install-inbound.mjs","find":"holdInstallSurfaceLockOrExit();","replace":";","killedBy":"PR C 第 2 步"},
  {"id":"m1538","file":"scripts/codex/install.mjs","find":"holdInstallSurfaceLockOrExit();","replace":";","killedBy":"PR C 第 2 步"},
  {"id":"m1539","file":"scripts/maintenance-install.mjs","find":"const surface = acquireInstallSurfaceLock({ home: c.home });","replace":"const surface = { ok: true, path: \"\", release: () => ({ ok: true }) };","killedBy":"PR C 第 2 步"},
  {"id":"m1540","file":"scripts/maintenance-gate.mjs","find":"surface = acquireInstallSurfaceLock({ home: c.home });","replace":"surface = { ok: true, path: \"\", release: () => ({ ok: true }) };","killedBy":"PR C 第 2 步"},
  {"id":"m1541","file":"scripts/maintenance/maintenance-install-core.mjs","find":"if (!held.ok || held.run !== \"held\") return { ok: false, reason: \"lease_not_held\"","replace":"if (false) return { ok: false, reason: \"lease_not_held\"","killedBy":"PR C 第 2 步"},
  {"id":"m1542","file":"scripts/maintenance/maintenance-install-core.mjs","find":"if (requireLease && (lease === null || typeof lease?.path !== \"string\")) return { ok: false, reason: \"lease_required\"","replace":"if (false) return { ok: false, reason: \"lease_required\"","killedBy":"PR C 第 2 步"},
  {"id":"m1543","file":"scripts/install-surface-lock.mjs","find":"if (!rel.ok) { err(\"安装面锁交不还（\" + String(rel.why) + \"，\" + String(rel.path) + \"）。\"); process.exitCode = 3; }","replace":"if (!rel.ok) { err(\"安装面锁交不还（\" + String(rel.why) + \"，\" + String(rel.path) + \"）。\"); }","killedBy":"PR C 第 2 步"},
  {"id":"m1544","file":"scripts/maintenance-gate.mjs","find":"if (!rel.ok) { out(\"安装面锁交不还（\" + String(rel.why) + \"，\" + String(rel.path) + \"）。\"); code = 3; }","replace":"if (!rel.ok) { out(\"安装面锁交不还（\" + String(rel.why) + \"，\" + String(rel.path) + \"）。\"); }","killedBy":"PR C 第 2 步"},
  {"id":"m1545","file":"scripts/maintenance-install.mjs","find":"if (!rel.ok) { out(\"安装面锁交不还（\" + String(rel.why) + \"，\" + String(rel.path) + \"）。\"); cliCode = 3; }","replace":"if (!rel.ok) { out(\"安装面锁交不还（\" + String(rel.why) + \"，\" + String(rel.path) + \"）。\"); }","killedBy":"PR C 第 2 步"},
  {"id":"m1546","file":"scripts/maintenance/maintenance-install-core.mjs","find":"if (held.reapUncleared) return { ok: false, reason: \"lease_reap_uncleared\", why: String(held.reapUncleared.error ?? \"\"), path: held.reapUncleared.path, token };","replace":"if (false) return { ok: false, reason: \"lease_reap_uncleared\", why: String(held.reapUncleared.error ?? \"\"), path: held.reapUncleared.path, token };","killedBy":"PR C 第 2 步"},
  {"id":"m1547","file":"scripts/maintenance/operation.mjs","find":"const surfaceLock = inspectInstallSurfaceLock({ home: ctx.home });","replace":"const surfaceLock = { holder: { state: \"absent\" }, residues: [], path: \"\" };","killedBy":"PR C 第 2 步"},
  {"id":"m1548","file":"scripts/install-surface-lock.mjs","find":"if (rel?.ok && rel.absent) return { ok: false, why: \"lock_lost","replace":"if (false) return { ok: false, why: \"lock_lost","killedBy":"PR C 第 2 步"},
  {"id":"m1549","file":"scripts/install-surface-lock.mjs","find":"if (rel?.reason === \"not_owner\") return { ok: false, why: \"lock_instance_replaced","replace":"if (rel?.reason === \"not_owner\") return { ok: true }; if (false) return { ok: false, why: \"lock_instance_replaced","killedBy":"PR C 第 2 步"},
  {"id":"m1550","file":"scripts/install-surface-lock.mjs","find":"isCanonicalIso(o.at) && typeof o.token === \"string\" && o.token.length > 0;","replace":"isCanonicalIso(o.at);","killedBy":"PR C 第 2 步"},
  {"id":"m1551","file":"scripts/install-surface-lock.mjs","find":"    if (err?.code === \"ENOENT\") names = [];","replace":"    if (true) names = [];","killedBy":"PR C 第 2 步"},
  {"id":"m1552","file":"scripts/install-surface-lock.mjs","find":"residues.push(UUID_SHAPE.test(tail.slice(\".reap.quarantine-\".length)) ?","replace":"residues.push(true ?","killedBy":"PR C 第 2 步"},
  {"id":"m1553","file":"scripts/install-surface-lock.mjs","find":"const transient = (s) => s.state === \"held\" && s.alive && s.ageMs <= staleMs;","replace":"const transient = (s) => false;","killedBy":"PR C 第 2 步"},
  {"id":"m1554","file":"scripts/outbound.mjs","find":"const r = readRegularFile(path.join(claimsDir, name));","replace":"const r = ((pp) => { try { return { status: \"read\", buf: fs.readFileSync(pp) }; } catch { return { status: \"unreadable\", why: \"读失败\" }; } })(path.join(claimsDir, name));","killedBy":"run 通道排空"},
  {"id":"m1555","file":"scripts/outbound.mjs","find":"账本不会自动动它：delivery-claims/\" + name,","replace":"账本不会自动动它：delivery-claims/\" + name.slice(0, 80),","killedBy":"run 通道排空"},
  {"id":"m1556","file":"scripts/outbound.mjs","find":"const LEGACY_CLAIM_ENTRY_RE = /^([0-9a-f]{64})\\.deliver_failed\\.json$/u;","replace":"const LEGACY_CLAIM_ENTRY_RE = /^([0-9a-f]{64})\\.deliver_failed.*$/u;","killedBy":"run 通道排空"},
  {"id":"m1557","file":"scripts/outbound.mjs","find":"const legacy = LEGACY_CLAIM_ENTRY_RE.exec(name);","replace":"const legacy = null && LEGACY_CLAIM_ENTRY_RE.exec(name);","killedBy":"run 通道排空"},
  {"id":"m1558","file":"scripts/outbound.mjs","find":"）—— 人工确认内容没有还要用的信息后删除或归档，账本不会自动动它：","replace":"）—— 可直接删：","killedBy":"run 通道排空"},
  {"id":"m1559","file":"scripts/control-command.mjs","find":"    && typeof owner.token === \"string\" && owner.token.length > 0;","replace":"    ;","killedBy":"Claude 真入口"},
  {"id":"m1560","file":"scripts/control-command.mjs","find":"try { st = fs.lstatSync(fullPath); }\n  catch (err) {\n    if (err?.code === \"ENOENT\") return { present: false };\n    return { present: true, shape: \"io_error\", why: String(err?.code ?? err?.message ?? err) };\n  }","replace":"try { st = fs.lstatSync(fullPath); }\n  catch (err) {\n    return { present: false };\n  }","killedBy":"Claude 真入口"},
  {"id":"m1561","file":"scripts/outbound.mjs","find":": \" —— 但形态不是本协议的制品（不是 symlink 或 payload 畸形）：先核验再处理，只人工处置，不要直接删\";","replace":": \" —— 可直接删\";","killedBy":"run 通道排空"},
  {"id":"m1562","file":"scripts/outbound.mjs","find":"const a = inspectControlLockArtifact(path.join(claimsDir, name));","replace":"const a = { present: true, shape: \"symlink_owner\" };","killedBy":"run 通道排空"},
  {"id":"m1563","file":"scripts/control-command.mjs","find":"catch (err) {\n    if (err?.code === \"ENOENT\") return { present: false };\n    return { present: true, shape: \"io_error\", why: String(err?.code ?? err?.message ?? err) };\n  }\n  let owner = null;","replace":"catch { raw = null; }\n  let owner = null;","killedBy":"Claude 真入口"},
];
